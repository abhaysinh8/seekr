# `seekr-cli`

Use `seekr --url http://localhost:4000 --api-key skr_... indexes list` or configure `SEEKR_URL` and `SEEKR_API_KEY`. Flags take precedence over environment variables, which take precedence over `~/.config/seekr/config.json`. Add `--json` for script-friendly output.
